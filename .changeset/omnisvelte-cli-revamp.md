---
'omni-svelte': minor
---

Add OmniSvelte CLI (`omni`) with full command suite: `init`, `add`, `generate`, `db`, `migrate`, `ui`, `doctor`, `install-dependency`, and dev script aliases (`build`, `test`, `lint`, `format`). Includes interactive prompts via @clack/prompts, Commander.js routing, schema-mode overrides, local package testing options, and stub roadmap commands.
