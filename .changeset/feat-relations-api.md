---
"omni-svelte": minor
---

feat(schema): introduce new inline relations API for Drizzle 1.0

Adds `field.reference(() => schema)` for foreign keys and `relation.*` for Drizzle ORM query relations.
